# WorkoutTracker 🏋️‍♂️📊

WorkoutTracker — мобильное приложение на **Kotlin + Jetpack Compose** для комплексного отслеживания тренировок, питания, шагов, веса тела и прогресса по достижениям.  
Приложение ориентировано на удобство, визуальную наглядность и сохранность данных (перезапуск / выход не сбрасывают прогресс).

---

## 🔧 Основные возможности

- Учёт и планирование **тренировок** с упражнениями и фото.
- Ведение **пищевого дневника** и норм КБЖУ.
- Раздел **аналитики**: шаги, погода, КБЖУ-графика, вес, топ упражнений.
- Магазин **статей** с баллами и системой покупок.
- Система **достижений** с прогрессом и сложностью.
- Настраиваемый **профиль** пользователя с аватаром и темами (7 штук).
- Валидация, подсказки, уведомления и сохранение состояния.

---

## 🏋️‍♂️ 1. Тренировки

Экран: **Тренировки**  
Логика: `TrainingViewModel`

**Функционал:**

1. **Создание тренировки**
   - Добавление новой тренировочной сессии.
   - Выбор/отображение даты.
   - Возможность добавить одно или несколько упражнений.

2. **Редактирование тренировки**
   - Изменение списка упражнений в рамках тренировки:
     - изменение веса;
     - изменение количества повторений и подходов;
     - изменение названия упражнения.

3. **Удаление тренировки**
   - Удаление тренировки целиком вместе с её упражнениями.
   - Защита от случайных действий (подтверждение / продуманное UI-поведение).

4. **Упражнения**
   - Просмотр актуального списка доступных упражнений.
   - Создание новых упражнений.
   - Возможность добавить **фото к упражнению** (например, фото тренажёра или техники):
     - выбор изображения из галереи;
     - хранение ссылки/пути в данных упражнения;
     - отображение превью внутри карточки.

**Валидация:**

- Название упражнения не пустое.
- Вес, повторения, подходы — числовые, без отрицательных значений.
- Корректная обработка отмены ввода, выхода и перезагрузки.

---

## 🍽 2. Питание

Экран: **Питание**  
Логика: `NutritionViewModel`

**Функционал:**

1. **Создание, редактирование и удаление приёмов пищи**
   - Добавление записи о приёме пищи:
     - название;
     - калории;
     - белки;
     - жиры;
     - углеводы;
     - масса (граммы).
   - Возможность отредактировать уже созданную запись.
   - Удаление ненужных записей.

2. **Задание нормы КБЖУ**
   - Пользователь может указать свои дневные нормы:
     - калорий;
     - белков;
     - жиров;
     - углеводов.
   - Эти нормы используются в разделе **Аналитика** для расчёта процентов.

3. **Карточка с текущими значениями КБЖУ**
   - Агрегация всех приёмов пищи за текущий день.
   - Отображение суммарных:
     - калорий;
     - белков;
     - жиров;
     - углеводов.
   - Сравнение с заданной нормой (в процентах).

4. **Валидация значений**
   - Проверка, что введены числа (с поддержкой точки/запятой при необходимости).
   - Защита от некорректных значений (отрицательные, слишком большие, пустые поля).
   - Уведомление/подсказки при ошибках.

---

## 📊 3. Аналитика

Экран: **Аналитика** — центр мониторинга прогресса.

### 3.1. Настройки аналитики

Отдельная кнопка **настроек** открывает диалог, где можно задать:

1. **Цель по шагам**
   - Ввод целевого количества шагов в день.
   - Валидация диапазона (например, 1000–50000).
   - Сохранение в настройках и учёт в уведомлениях.

2. **Город для погоды**
   - Ввод города для получения погодных данных.
   - Сохранение значения для последующего автообновления.

3. **Включение/выключение уведомлений**
   - Флаг `вкл/выкл` для уведомлений о достижении цели по шагам.
   - При выключении уведомления больше не приходят, логика учитывает состояние флага.

4. **Количество топ упражнений**
   - Настройка, сколько лучших упражнений показывать в блоке «Топ результатов».
   - Диапазон (например, от 1 до 10).

5. **Обновление погоды**
   - Кнопка «Обновить погоду сейчас» форсирует обновление с сервера вне очереди.

---

### 3.2. Карточка прогресса шагов

**Функционал:**

- Отображение текущего количества шагов за день и цели: `текущие шаги / цель`.
- Визуальный прогресс-бар с плавным заполнением.
- Если не выдано разрешение `ACTIVITY_RECOGNITION`:
  - показывается кнопка «Разрешить»;
  - по нажатию запрашивается разрешение.

**Уведомление о достижении цели:**

- При достижении или превышении цели:
  - отправляется уведомление:
    - с иконкой приложения,
    - с текстом: **«Вы прошли X шагов из цели Y»**;
  - для каждой цели/дня уведомление приходит только один раз.
- Учитывается:
  - включён ли флаг уведомлений;
  - есть ли разрешение на уведомления (POST\_NOTIFICATIONS для Android 13+).

---

### 3.3. Погода

**Функционал:**

- Получение текущей погоды по указанному городу.
- Использование русского языка для описания (параметр `lang=ru`).
- Показ:
  - температуры (в °C);
  - текстового описания погоды (например, «Ясно», «Небольшой дождь»);
  - времени последнего обновления.

**Автообновление и кэширование:**

- Ответ от API кешируется в `SharedPreferences` (температура + описание + время).
- При открытии экрана:
  - если данные свежие — используется кэш;
  - если устарели — отправляется новый запрос.
- При отсутствии сети:
  - если есть кэш — используются старые данные + подсказка пользователю;
  - если кэша нет — выводится понятное сообщение.

---

### 3.4. Кольцевые диаграммы КБЖУ

**Функционал:**

- По данным из раздела **Питание** за текущий день строятся четыре диаграммы:
  - Калории,
  - Белки,
  - Жиры,
  - Углеводы.
- Для каждой диаграммы:
  - вычисляется процент от дневной нормы;
  - отображается подписанный круговой индикатор:
    - внутри — текущий %;
    - снизу — краткая подпись (Кал, Б, Ж, У).

---

### 3.5. График прогресса веса

**Функционал:**

- Карточка ввода текущего веса:
  - поле ввода (кг);
  - кнопка «Сохранить»;
  - валидация диапазона (например, 30–300 кг) и формата числа.
- История веса:
  - хранится в виде набора точек (дата + вес);
  - ограничение на количество точек (например, последние 60 записей).

**График:**

- Ось X — даты (формат `ДД.MM`);
- Ось Y — значения веса;
- Точки соединены линией, визуализируя изменение веса со временем.
- Для подписей по X используется до 3 точек (начало, середина, конец), чтобы график оставался читаемым.

---

### 3.6. Топ лучших результатов

**Функционал:**

- Анализ всех тренировок и упражнений.
- Для каждого упражнения считается «сила» подхода (например: `вес × повторения × подходы`).
- На основе этого выводится **топ лучших результатов**:
  - сортировка по убыванию результата;
  - количество берётся из настроек («Количество топ упражнений»).
- Отображаются:
  - название упражнения;
  - лучший сет (например, `100 кг × 5 × 3`).

---

## 📚 4. Статьи

Экран: **Статьи**  
Логика: `ArticleViewModel`

**Функционал:**

1. **Отображение текущего количества баллов**
   - В верхней части или в шапке раздела показывается текущий баланс баллов пользователя.
   - Баллы тратятся на покупку статей.

2. **Поиск по заголовку**
   - Поле поиска фильтрует список статей по названию.
   - Поиск работает «на лету» при изменении текста.

3. **Карточки статей с предпросмотром**
   - Каждая статья отображается в виде карточки:
     - заголовок;
     - краткое описание / отрывок;
     - часть контента для предпросмотра (без покупки — ограниченный доступ).
   - Визуально оформлены (иконки/иллюстрации, отступы, тени).

4. **Покупка и статус статьи**
   - Для каждой статьи:
     - если ещё не куплена — отображается стоимость и возможность купить;
     - если куплена — показывается статус (например, «Куплено») и полный доступ к содержимому;
     - если баллов не хватает — кнопка покупки недоступна / отображается сообщение.
   - Логика покупки:
     - проверка баланса;
     - при успехе:
       - обновление остатка баллов;
       - пометка статьи как купленной.

---

## 🏅 5. Достижения

Экран: **Достижения**  
Логика: `AchievementViewModel`

**Функционал:**

1. **Счётчик выполненных достижений**
   - В верхней части раздела показывается общее количество завершённых достижений (например, `3/10`).

2. **Карточки достижений**
   - Для каждого достижения:
     - название;
     - описание;
     - прогресс (например, `60%` или `3/5`);
     - визуальный прогресс-бар;
     - индикатор завершённости (цвет, иконка, состояние).

3. **Сложность достижений**
   - Для каждого достижения задаётся «уровень сложности», отображаемый звёздами:
     - 1 ⭐ — простое достижение;
     - 2 ⭐⭐ — среднее;
     - 3 ⭐⭐⭐ — сложное / долгосрочное.
   - Звёзды показываются прямо на карточке (визуальная группировка по сложности).

---

## 👤 6. Профиль

Экран: **Профиль**  
Логика: `AuthViewModel` + локальное состояние

**Функционал:**

1. **Настройка тем приложения (7 тем)**
   - В профиле доступна кнопка переключения темы приложения.
   - Всего поддерживается **7 разных тем** (например: системная, светлая, тёмная, тёплая, фуксия и другие варианты).
   - Выбранная тема:
     - сохраняется в настройках;
     - восстанавливается при следующем запуске;
     - применяется ко всем экранам.

2. **Выход из аккаунта**
   - Кнопка «Выйти»:
     - очищает авторизационные данные;
     - переводит пользователя на экран логина;
     - очищает стек навигации, чтобы нельзя было вернуться назад в приложение через «Назад».

3. **Информация о пользователе**
   - Просмотр данных:
     - email;
     - имя;
     - фамилия;
     - возраст;
     - пол;
     - параметры фигуры (рост, вес, плечи, талия, бёдра, грудь);
     - цель и дедлайн цели;
     - дата измерений.
   - Редактирование:
     - по кнопке «Изменить» для каждого поля открывается диалог ввода;
     - валидация (формат email, диапазоны чисел, корректность дат);
     - изменения сохраняются через `AuthViewModel.updateUser`.

4. **Фото профиля (аватар)**
   - Выбор изображения из галереи.
   - Копирование в локальное хранилище приложения.
   - Автоматическая замена старого аватара (удаление старого файла).
   - Отображение круглого аватара с кнопкой камеры поверх для смены.

5. **Интерактивная кнопка для сохранения**
   - Внизу экрана — фиксированная кнопка «Сохранить изменения».
   - Активна только если данные были изменены.
   - По нажатию:
     - всё введённое пользователем сохраняется;
     - показывается уведомление/подсказка об успешном сохранении.

---

## ✅ UX / Надёжность

Во всех разделах реализовано:

- **Валидация данных**
  - числовых полей (шаги, вес, КБЖУ, цели по шагам);
  - форматов дат;
  - форматов email и прочих строковых значений.

- **Подсказки и уведомления**
  - Snackbar-сообщения при ошибках и успешных действиях;
  - понятные сообщения «что пошло не так» (сеть, ввод, права).

- **Сохранение состояний**
  - настройки и ключевые данные сохраняются в `SharedPreferences` / БД;
  - перезагрузка приложения или выход не приводит к потере данных.

- **Защита от случайных действий**
  - аккуратная работа с удалением;
  - невозможность нажать «Сохранить», если данные некорректны;
  - проверка разрешений перед доступом к шагам и уведомлениям.

---

## 🛠 Технологический стек

- **Kotlin**
- **Jetpack Compose (Material 3)**
- **Navigation Compose**
- **ViewModel + StateFlow**
- **Room / локальное хранилище** (для тренировок, питания, статей)
- **Retrofit + Gson** (погода)
- **SharedPreferences** (настройки, кэш)
- **Sensors API (TYPE_STEP_COUNTER)** (шаги)
- **NotificationCompat** (уведомления)
- **Coil** (загрузка и отображение изображений для аватара и упражнений)

